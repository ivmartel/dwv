var parse = require("url").parse;
var fetch = require("./fetchFromUrl.js");

fetch(parse(process.argv[2]), function(err, str, filename) {
    var json = err ? [err.message] : [null, str, filename];
    process.stdout.write(JSON.stringify(json), process.exit);
});

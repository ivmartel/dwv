const requireFromUrl = require('./index.js');

module.exports = async function(urls) {
    return new Promise(function(resolve, reject) {
        requireFromUrl(urls, function(err, ...m) {
            !err ? resolve(m) : reject(err);
        });
    });
}
